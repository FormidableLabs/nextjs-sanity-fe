// import type * as urql from "next-urql";

// /**
//  * Same as SSRData, but tracks the data type.
//  * This helps E2E tests infer the correct type.
//  */
// export type SSRData<TQuery> = urql.SSRData & { TQuery?: TQuery };
// /**
//  * Same as WithUrqlState, but tracks the data type
//  * This helps E2E tests infer the correct type.
//  */
// export type WithUrqlState<TQuery> = { urqlState: SSRData<TQuery> };
