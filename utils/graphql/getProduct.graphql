query getProduct($slug: String!) {
  allProduct(where: { slug: { current: { eq: $slug } } }) {
    _id
    name
    categories {
      name
    }
    slug {
      current
    }
    images {
      images {
        asset {
          url
        }
      }
      name
    }
    variants {
      id
      name
      description
      msrp
      price
      size {
        name
      }
    }
  }
}
